import { RoutePlaceholder } from '@/pages/RoutePlaceholder';

/**
 * `/seller` — painel do vendedor: peças por status e totais (FE-US019-1, #220).
 * Placeholder mínimo; a tela real substitui isto por inteiro.
 */
function SellerAdmin() {
  return <RoutePlaceholder title="Painel do vendedor" />;
}

export default SellerAdmin;
