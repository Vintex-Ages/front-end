import { RoutePlaceholder } from '@/pages/RoutePlaceholder';

/**
 * `/sell` — "Quero vender": fluxo de criação da loja (FE-US006-1, #212).
 * Placeholder mínimo; a tela real substitui isto por inteiro.
 */
function Sell() {
  return <RoutePlaceholder title="Quero vender" />;
}

export default Sell;
