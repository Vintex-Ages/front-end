import { RoutePlaceholder } from '@/pages/RoutePlaceholder';

/**
 * `/cart` — página do carrinho, com aviso de peça indisponível (FE-US021-2, #226).
 * Placeholder mínimo; a tela real substitui isto por inteiro.
 */
function Cart() {
  return <RoutePlaceholder title="Carrinho" />;
}

export default Cart;
