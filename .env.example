# URL base da API do back-end (repositório separado)
VITE_API_BASE_URL=http://localhost:3000/api

# "true" usa o mock em src/mocks/products.ts; "false" chama a API real acima.
# Fica "true" por padrão porque o back ainda não tem endpoint de busca/detalhe.
VITE_USE_MOCKS=true
